---
name: reference-repos
description: 비자명한 설계·접근 결정 전에, 비슷한 문제를 이미 해결해 둔 기존 git 프로젝트(prior art)가 있는지 찾아 그 해법을 참고한다(읽기). 또한 나중에 참고 가치가 생긴 기법을 인덱스에 자동 기록한다(쓰기 — CLAUDE.md 4-c: 어렵게 뚫은 해법/재사용 기법 발생 시 묻지 말고 repos.json에 append). "예전에 비슷한 거 어디서 해봤지", "기존 프로젝트 참고해줘", "그 프로젝트 참고해봐", "내 프로젝트 중 참고할 거 훑어봐", "github에서 관련 프로젝트 검수해봐", "prior art 찾아줘", "reference에 기록해줘", 또는 새 기능 구현 접근을 정할 때·스턱루프가 성공으로 풀렸을 때 사용. 메인 신호는 사용자가 이름으로 지목한 repo. 인덱스는 ~/.claude/repos.json의 reference/remote 필드이고, 모자라면 GitHub 공개 API로 소유 repo를 라이브 스캔(gh 불요). 경로는 PC마다 달라도 git remote로 PC 독립 접근(로컬 클론 우선, 없으면 shallow clone 캐시).
---

# reference-repos — 완성된 프로젝트에서 prior art 참고

새 작업에서 비자명한 설계·접근을 정하기 전에, **이미 비슷한 문제를 풀어 둔 내 git 프로젝트**가 있는지 찾아 그 해법을 근거로 삼는다. git에 올라간 repo는 어느 정도 완성된 것이 많아 참고 가치가 높다. 경로는 PC마다 다르거나 없을 수 있지만 **git remote는 항상 정확**하므로 remote를 키로 접근한다.

## 인덱스 = `~/.claude/repos.json`

각 항목의 두 필드를 본다(sync-repos와 공용 파일이지만 sync는 `path`/`build`만 읽으므로 무관):

- `remote` — git URL. **PC 불문 안정 키.**
- `reference` — "이 repo가 잘 풀어 둔 문제" 한 줄. **이게 있는 항목만 참고 대상.** (없으면 단순 동기화용 repo.)
- `path`(선택) — 홈(`~`) 기준 상대경로. 이 PC에 로컬 클론이 있을 때의 위치 힌트.

`reference`가 있고 `path`가 없는 항목 = **참고 전용 repo**(sync-repos는 자동 제외함).

## 절차

1. **인덱스 로드** — `~/.claude/repos.json`을 읽어 `reference` 있는 항목만 추린다.

2. **지목 우선 + 인덱스 매칭** — 가장 신뢰도 높은 신호는 **사용자가 이름으로 지목한 repo**다(만든 사람이 기능을 가장 잘 안다). 지목이 있으면 그걸 1순위로. 더해서, 지금 풀려는 문제를 각 `reference` 노트와 대조해 관련 repo를 고른다.

3. **라이브 GitHub 스캔(놓침 줍기·인덱스 보강)** — 인덱스 매칭이 비었거나 얕을 때, 또는 사용자가 "GitHub에서/내 프로젝트 중 참고할 거 훑어봐"처럼 요청하면, 소유자의 repo 목록을 실시간으로 받아 인덱스에 없던 prior art까지 포괄한다:
   - **username 추출**: `repos.json`의 remote(`github.com/<user>/...`)나 `git config`에서 뽑는다.
   - **공개 repo (토큰 불요)**: `curl -s "https://api.github.com/users/<user>/repos?per_page=100&sort=updated"` → 이름·설명·언어로 후보를 추린다. `gh` 미설치 PC에서도 동작.
   - **비공개 repo**: 익명 API엔 안 뜬다 → `gh`(설치 시)나 토큰이 있어야 보임. 없으면 "공개분만 훑음, 비공개는 직접 지목 요망"으로 알린다.
   - GitHub 설명이 비어 이름만으론 거칠면, 관련돼 보이는 후보를 **사용자에게 제시**("이것들 관련 있어 보임 — 볼까요?")하거나 README를 가볍게 열어 확인한다. 추측으로 단정하지 않는다.

4. **코드 확보(PC 독립)** — 고른 repo의 실제 코드를 연다:
   - **로컬 클론 우선**: `path`가 있으면 `~/<path>` 확인. 없거나 `path`가 비었으면, repo 이름으로 흔한 개발 폴더(`~/Dev`, `~/Documents` 등)에서 `.git` remote가 일치하는 클론을 가볍게 탐색.
   - **없으면 shallow clone 캐시**: `git clone --depth 1 <remote> ~/.claude/ref-cache/<repo-name>` 로 받아 읽는다. 이미 캐시에 있으면 재사용(필요 시 `git -C <cache> pull --ff-only`로 갱신). 캐시 디렉터리는 읽기 전용 참고용이며 수정하지 않는다.
   - clone 실패(네트워크·인증)면 그 사실을 보고하고 나머지 후보로 진행.

5. **해법 추출** — 관련 파일을 읽어(README/PLAN/HANDOFF 같은 설계문서가 있으면 먼저, 그다음 핵심 소스) **현재 문제에 옮길 수 있는 접근**을 요약한다. 그대로 베끼지 말고, 왜 그렇게 풀었는지·우리 상황과 다른 점·옮길 때 바꿔야 할 부분을 함께 제시한다.

6. **판단 보조 + 인덱스 자가성장** — 추출한 접근을 현재 작업의 선택지·추천에 **근거로** 연결한다(prior art가 정답이라는 보장은 없으니 맹신 말고 트레이드오프를 함께 평가). 라이브로 찾아 *실제로 참고한* repo가 인덱스에 없으면 아래 «기록(쓰기)» 절차로 자동 등록한다(미리 카탈로그화하지 않고 **사용에서 인덱스가 자라게**).

## 기록(쓰기) — 묻지 말고 자동, 한 줄 고지

읽기(위 절차)의 짝. 나중에 참고 가치가 생기면 `repos.json`을 **자동 갱신**한다(CLAUDE.md 규칙 4-c). 사용자에게 묻지 않고, 한 줄만 고지: «📚 reference 기록 — {repo}: {기법 한 토막}». 사용자는 다음 턴에 취소할 수 있다.

### 언제 (두 트리거)
- **Type A — 무조건.** 규칙 11-b 스턱루프가 *성공으로 풀린 순간*(안 되다 안 되다 드디어 됨). 어렵게 뚫었으니 값이 크다. *그 순간 바로* 기록(push까지 미루면 '왜 어려웠나'가 증발).
- **Type B — 품질바 통과 시만.** 재사용 가치 있다고 판단되는 비자명한 구체 기법. **"구글 5분이면 나오는 일반지식·트리비아"는 제외**(skillify와 같은 바). 애매하면 안 넣는다(보수적).

### 무엇을 (append 우선, 함정→해법 밀도)
- **기존 repo에 append가 기본.** 보통 *이미 인덱스에 있는* repo 안에서 작업하므로, 그 항목의 기존 `reference` 문자열 **끝에 그 기법 키워드를 덧붙인다**. 새 repo 항목 생성은 그 repo가 인덱스에 아예 없을 때만.
- **dedup** — 이미 있는 키워드는 다시 넣지 않는다.
- **Type A는 함정→해법을 통째로.** 값은 '왜 어려웠나'에 있다. 기존 항목들처럼(예: PasteFlow "클릭통과면 커서밑 앱이 WM_SETCURSOR로…벗겨지므로 오버레이가 입력을 소유") **막다른 길·왜 그게 안 됐나·최종 해법**을 인라인으로 박는다. 결과만 적지 말 것.
- `reference`는 **grep 매칭용 키워드 봉지** — 스키마를 객체로 쪼개지 말고 납작한 문자열 유지. 키워드(기능·기법·함정 이름)를 충분히.

### 새 repo 항목이 필요할 때
```json
{ "desc": "프로젝트 설명 [참고 전용]", "remote": "https://github.com/.../repo.git",
  "reference": "이 repo가 잘 풀어 둔 문제들 — 함정→해법·키워드 위주로" }
```
- 이 PC에서 동기화도 하고 싶으면 `path`(+`build`)를 같이 넣는다(sync-repos 명단과 통합).
- `.claude` repo가 첫 항목이라 push 해두면 다른 모든 PC에 인덱스가 자동 동기화된다.

### 커밋 번들
`repos.json` 변경은 별도 커밋을 만들지 말고, **push 시 그 커밋에 흡수**한다(doc-sync가 push 전 안전망으로 한 번 더 줍는다 — CLAUDE.md 규칙 10과 같은 패턴).

## 안전 가드

- 캐시 클론(`~/.claude/ref-cache/**`)과 기존 로컬 클론은 **읽기 전용 참고용** — 수정·커밋·push 하지 않는다.
- `repos.json` 갱신은 위 «기록» 트리거(Type A/B) 하에서 **자동**이다(그 외 임의 편집은 사용자 요청 시). 코드 파일·다른 repo·git mutating 명령은 건드리지 않는다.
