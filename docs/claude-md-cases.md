# CLAUDE.md 실패사례 상세 (참고용 — 세션 시작 시 자동 로드 안 됨)

`~/.claude/CLAUDE.md`는 매 세션 전량 로드되지만 이 파일은 아니다. CLAUDE.md 쪽 규칙 옆엔 압축된 한 줄 + `(상세: cases.md#앵커)` 포인터만 남기고, 전체 서사(무슨 일이 있었고 왜 실패했나)는 여기 보존한다. 규칙의 "왜"를 더 깊이 알고 싶을 때만 Read로 연다.

---

## c1-arrow-diff (규칙 1-b, 비교형 증상)
2026-08-03, Easy CAD 화살촉 렌더: "직각 화살표만 머리 어깨가 깎인다(곡선·직선은 멀쩡)"는 결정적 단서를 받고도, 재현 시나리오를 두 번 더 늘렸다(도형에 바인딩·곡선+바인딩). 정상 클래스(`_ArrowItem`)와 버그 클래스(`_PolyArrowItem`)의 화살촉 그리기 코드 두 줄을 나란히 읽는 건 세 번째 재현마저 실패한 뒤에야 했다. 대조하자 원인(`QPen` 기본 `joinStyle`이 `BevelJoin`인데 한쪽만 `RoundJoin`을 명시)이 그 자리에서 바로 나왔다.

## c2-gateway-search (규칙 2, 손안의 카드)
게이트웨이 문서를 이미 읽었고 DuckDuckGo를 실측까지 했는데도, "검색 제공자를 뭘로 할까요?"를 *외부* API 목록(DuckDuckGo·Tavily·Brave)으로만 채웠다. 정답(내장 웹검색을 중계하는 Responses API)은 이미 손안에 있었고, 잡아낸 건 규칙이 아니라 사용자였다. **탐색은 프레임의 하류에 있다** — "어느 외부 API를 쓸까"로 문제가 굳은 뒤엔 규칙 1이 그 프레임 *안에서* 성실히 조사할 뿐이다. 더 나쁜 건 그 성실함이 열등한 선택지에 *근거와 추천 딱지*를 달아 준다는 것. 조사를 안 했으면 "확실치 않다"고 말했을 텐데, 조사를 했기에 확신을 갖고 틀렸다. (비유: 지도를 꼼꼼히 읽었는데 애초에 펼친 게 옆 동네 지도 — 독도법이 정확할수록 더 멀리 간다.) 그래서 트리거를 *답*이 아니라 *선택지를 만드는 순간*에 건다.

## c3-list-renumber (규칙 7, 목록 렌더링)
2026-07-11: 마크다운 순서목록은 렌더러가 들여쓰기를 재해석한다 — 순서목록 `1.` 아래 자식 줄이 다시 `1.`로 번호 매겨져 상하위가 뭉개졌다. 코드블록은 찍은 공백이 그대로 나와 렌더러 리플로우에 면역이라 구조가 중요할수록 안전하다.

## c4-session-state-oneline (규칙 7, 산출물 강제 형식)
2026-07-11: 규칙 10-b의 "세션 상태" 산출물을 *한 줄*로 정의한 탓에, 남은 일 3건과 각각의 근거가 가운뎃점으로 이어진 거대한 한 줄이 됐다. 정보는 다 있었지만 읽히지 않았다. → 산출물을 강제할 땐 그 형식이 '읽히는 형식'인지도 함께 정해야 한다. 강제할 것은 *내용*이지 *한 줄*이 아니다.

## c5-auto-cleanup-hook (규칙 8, 구조 걸림)
2026-08-02, Easy CAD: 자동 훅으로 미리 정리를 걸자는 제안이 "사용자가 항상 yes 할 것"이라는 지적으로 기각되고, 대신 `🧱 구조 걸림:` 보고 방식으로 대체됐다.

## c6-presync-zero (규칙 10, 사전 doc-sync)
2026-07-12 세션에서 push 4회 중 사전 doc-sync 0회 — 기억 의존 단계는 죽는다는 맨 위 형태론 그대로였다. 그래서 PreToolUse 훅으로 기계 강제(센티널 파일)로 전환했다.

## c7-autocommit-revert (규칙 10-a, 자동커밋 되돌림)
자동커밋은 "커밋할까요?" 대화 한 번을 아꼈지만, `~/.claude`처럼 한 세션 안에서 소소한 편집이 잦은 레포에서는 커밋 단위가 편집 리듬을 그대로 반영해 잘게 쪼개져 히스토리가 지저분해졌다(예: 2026-07-11 세션의 연속 다듬기가 별개 커밋 5개로 흩어짐). "검증 끝난 의미있는 단위에서만 자동 커밋"이라는 절충안도 검토했으나, 그 판단을 내가 매번 내려야 해 맨 위 규칙 형태론("내가 판정하는 트리거는 트리거가 아니다")과 충돌해 기각했다.

## c8-template-gravity (규칙 10-b, 템플릿이 프로즈를 이긴다)
2026-07-11: 규칙 7에 프로즈로만 있던 규정(목록 항목 사이 빈 줄·프롬프트 라벨 등)이 10-b 출력에서 반복 누락됐다 — 원인은 성향 부족이 아니라 템플릿이 위반을 모델링했기 때문이다(item 2에 라벨 없음·항목 사이 빈 줄 없음). 가장 가까운 구체 규격(템플릿)의 중력이 프로즈 규칙을 이긴다. 그래서 프로즈를 더 쓰지 않고 템플릿 자체를 고쳤다.

## c9-manual-numbering (규칙 10-b, 수동 번호)
2026-07-11(위와 같은 날 2차): 권장 항목을 순서목록(`1.`)으로 쓰자 렌더러가 그 아래 `프롬프트:` 줄까지 같은 레벨 항목으로 보고 번호를 다시 매겨, 부모도 `1.` 자식도 `1.`로 찍혀 상하위가 뭉개졌다. 수동 번호(`**1.**`)는 리스트 문맥 자체를 만들지 않아 자식이 번호를 훔치지 못한다.

## c10-prompt-overproduction (규칙 10-b, 프롬프트 시점)
2026-07-13: 실검증 `[지금]` 1건과 함께 후속 설계용 새-세션 프롬프트 전문을 같은 답에 실었다 — 검증이 실패하면 그 프롬프트는 폐기될 물건이었고, 사용자에겐 "프롬프트 남발"로 읽혔다. 부작용(사용자가 아무것도 안 누르고 세션을 닫으면 새-세션 프롬프트가 안 남음)은 감수한다 — 그 경우 `[지금]` 결과가 없어 어차피 프롬프트가 틀렸을 것이다.

## c11-heading-axis (규칙 10-b, 헤딩 축 두 번의 실패)
2026-07-11, 두 번: ⓐ 헤딩이 "남은 일 있음"이라 남은 모든 일(8월 재감사·웹 UI 프롬프트 개정)을 쏟아부었다 — 붙여넣어도 진행되지 않는 것들이었다. ⓑ 그걸 고치며 축을 "다음 세션이냐"로 잡았더니, 이번엔 붙여넣으면 멀쩡히 작동하는 「다른 PC 세션」 작업이 제목과 어긋났다. 축은 *언제/어디*가 아니라 「프롬프트가 작동하는가」다.

## c12-scratchpad-loss (규칙 10-b, 왜 — 휘발성 자산)
2026-07-11: 감사 스크립트가 임시폴더에 있어 곧 소실될 상황에서 푸시부터 제안했다. 사용자는 "끝났구나"로 읽었고, 되물어보지 않았으면 그대로 날아갔다. 할 일이 남았는지 여부는 사용자가 추론할 게 아니라 내가 말할 것.

## c13-xgate-filter (규칙 11-b, 부재 확인)
2026-08-01, Easy CAD 정렬 가이드: 로그에 게이트 플래그(`x_gate`)를 직접 심어 두고도, 기능이 발화한 줄(`'adj'`가 찍힌 줄)만 grep해 "정상 작동 중"이라고 사용자에게 보고했다. 진짜 증거는 `x_gate=False`라서 후보에 오르지도 못해 안 찍힌 줄이었고, 그 안에는 두 변이 정확히 `0.00`으로 일치했는데 차단된 프레임까지 있었다. 계측은 옳았는데 그 계측값으로 필터링을 안 해서 스턱루프가 3라운드 더 이어졌다. 로그를 열면 성공 사례가 먼저 눈에 띄므로, 부재를 조사할 땐 먼저 전제조건 플래그로 필터링한다.

## c14-offscreen-habit (규칙 11-d, 자체확인 습관)
2026-07-31, Easy CAD: 속성 패널이 창 크기에 따라 깨지는 버그를 사용자에게 `python run.py`+콘솔 로그 붙여넣기를 여러 차례 요청해가며 진단했다. 사용자가 "니가 직접 프로그램을 키고 로그를 볼 수는 없냐"고 지적한 뒤에야, `QT_QPA_PLATFORM=offscreen`만 빼고 같은 재현 스크립트를 Bash로 직접 실행하면 사용자와 똑같은 실제 창·로그를 그대로 얻을 수 있음을 깨달았다 — 그 뒤로는 스스로 반복 재현하며 몇 분 만에 진짜 원인을 확정했다. 오프스크린 습관이 "GUI 검증은 사용자 몫"이라는 성급한 일반화를 만들었던 것 — 오프스크린이 못 하는 건 헤드리스 렌더링 한계(폰트·네이티브 위젯 특정 동작)뿐이지, 로컬 데스크톱에서 실제 창을 띄우는 것 자체는 막힌 적이 없었다.

## c15-prepush-filter (규칙 10, 사전 훅 과차단)
2026-08-10: c6에서 도입한 사전 doc-sync 훅이 "거의 모든 프로젝트, 거의 매 push"마다 걸린다는 사용자 체감을 받았다. 원인은 훅이 push 명령이 있으면 무조건 deny했다는 것 — 대칭인 post-push 훅(`doc-sync-hook.py`)은 이미 "푸쉬 범위에 비-문서 변경이 1개도 없으면 침묵"이라는 필터를 갖고 있었는데 사전 훅에는 없었다. 그 필터를 사전 훅에도 이식하고, 저장소에 동기화 후보 문서(`CLAUDE.md`/`docs/**/*.md`/루트 `*.md`/`.doc-sync.json`) 자체가 없는 경우까지 자동 통과 조건을 넓혔다 — 두 조건 다 doc-sync 스킬이 결국 "변경 없음"으로 끝날 게 결정적인 경우라 보호 수준 손실 없이 걸러진다. 단, 저장소에 `README.md` 하나만 있어도 "후보 있음"으로 판정해 여전히 걸린다 — 이건 의도한 보수적 설계(휴리스틱만으로 "이 README는 진짜 설계문서가 아니다"를 판단할 수 없어서)라 완전한 해소는 아니다.
