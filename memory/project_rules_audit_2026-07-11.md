---
name: project-rules-audit-2026-07-11
description: 전역 CLAUDE.md 규칙 감사(173세션) 결과와 개편 후 남은 후속 작업 — 재감사(2026-08 중순)·pick-skill 거취
metadata: 
  node_type: memory
  type: project
  originSessionId: d18b6971-6c01-426c-9656-0a2b6c70e529
---

2026-07-11, `~/.claude/projects`의 173개 세션(179MB)을 전수 파싱해 전역 CLAUDE.md 각 규칙의 실제 발화율을 규칙 추가일 전후로 측정했다. 결과로 규칙을 개편해 푸시(`27a57b0`, repo `mw3love/claude-global-config_260502`). 같은 날 저녁 2차로 10-a·10-b를 추가해 **현재 규칙 19개**(1·2·3·4-b·4-c·6·7·7-b·8·9·9-b·9-c·10·10-a·10-b·11·11-b·11-c·12).

**핵심 발견:** 규칙의 발화 여부를 가른 건 내용·중요도·개수가 아니라 **형태**다. ⓐ 훅(기계적 강제)=doc-sync 114회로 최다 ⓑ 산출물 강제(라벨·"추천:"·트레일러)=세션당 다수 ⓒ 성향 문장 + 내가 판정하는 트리거 = 거의 0회(pick-skill 라우팅 173세션 0회, 스턱루프 트립와이어 1회 vs 실제 스턱루프 35건). 가장 깨끗한 인과 증거는 규칙 11 self-review 자동실행: 도입(6/24) 전 사용자 수동 요구 38회·자동 0 → 도입 후 사용자 요구 0·자동 13. 성과 지표가 규칙의 어휘와 독립이라 유일하게 신뢰 가능.

**후속 작업 상태:**

1. **재감사 (미착수 — 2026-08 중순)** — 개편이 실제로 발화했는지는 아직 **미검증**. `python ~/.claude/tools/audit_rules.py`를 돌려 같은 잣대(발화율 전후 비교)로 재측정. 감사 대상 지문 7개가 스크립트에 이미 심겨 있다: 규칙2 `손안의 카드` / 11-b `🔁 스턱루프` / 7 `검토한 대안:` / 10-b `세션 상태:` · `커밋 상태:` · `프롬프트:` 라벨 · 선택창 `지금 이어서`. **발화 0이면 그 규칙도 죽은 것 — 옛 규칙 4·6-b를 자른 것과 같은 기준으로 자른다(규칙을 늘리지 말고 줄이는 방향).** ⚠ 2026-07-11 당일 실행 시엔 전부 `⏳ 판정보류(도입 후 0세션)`로 나온다 — 감사 창이 `added <= d < TODAY`라 당일을 제외하기 때문. 정상이며, 8월에 돌리면 잡힌다.

2. ~~감사 스크립트 영구화~~ **(완료, 2026-07-11)** — `~/.claude/tools/audit_rules.py`. **함정 4개(전부 실측으로 데임):**
   - ⓐ **단순 grep은 전부 오염된다.** 스킬명·규칙 본문이 *모든 세션의 시스템 프롬프트에 주입*되어 150+로 뜬다 → `type=="assistant"`의 text·tool_use 블록만 추출해야 한다.
   - ⓑ 슬래시 명령(user 턴 `<command-name>`)과 Claude 자발 스킬 호출(`tool_use` name=Skill)은 **겹치지 않는다.** 규칙 11의 인과를 증명한 핵심이라 섞으면 결론이 무너진다.
   - ⓒ **도구를 `("skill", …)`로 적으면 영영 0이다.** `skills`는 `name=="Skill"`인 tool_use만 담고 `input.skill`로 센다. `AskUserQuestion`처럼 이름이 곧 도구인 것은 안 잡혀 **'사문화' 오판 → 멀쩡한 규칙을 자른다.** → `("tool", …)` 종류를 새로 만들어 씀(2026-07-11 저녁).
   - ⓓ **넓은 지문은 '거짓 살아있음'을 만든다.** 굵은 수동 번호(`\*\*\d+\.`)는 추가 前 이미 200회, `AskUserQuestion` 통째는 260회 — 규칙이 죽어도 높게 나온다. 지문은 **그 규칙에만 있는 문구**로 좁혀야 한다(선택창은 질문 문구 `지금 이어서`로 좁힘). 죽은 규칙보다 나쁜 게 거짓 안심이다.
   - ⓔ 출력에 `⏳✓✗`가 섞여 **Windows cp949 콘솔에서 UnicodeEncodeError로 죽는다** → `sys.stdout.reconfigure(encoding="utf-8")` 필수(스크립트에 반영됨).

3. **규칙 11-b 부분 외부화 (완료, 2026-07-11)** — `~/.claude/stuck-loop-hook.py`(UserPromptSubmit). 훅이 transcript를 파싱해 좌절 **어휘** 2회째를 세어 주입. 리셋 앵커 = `🔁`(스턱루프/스킵 둘 다).
   **핵심 한계:** 어휘 기반이라 **recall이 낮다** — 173세션 리플레이에서 초안(A군∧B군 결합)은 실제 스턱루프 35건 중 **3건만** 발화, 재설계(TIER1 강한 좌절어 단독 ∪ TIER2 반복어×부정어) 후 **6건**(≈17%). 어휘 없이 중립 재보고된 스턱루프는 원리적으로 못 잡는다. → 규칙 문구를 "훅이 강제"에서 **"안전망(훅 침묵 ≠ 스턱루프 아님)"**으로 낮췄다. 완전 외부화는 어휘로는 불가.
   ⚠ **검증 상태 정정(2026-07-11 저녁):** 그날 저녁 합성 transcript 5개 시나리오(중립·1회째 침묵·2회째 발화·리셋 후 침묵·TIER2)를 돌려 전부 통과시키고 "실조건검증"이라 표기했으나 **그건 과장이다.** 내가 만든 좌절 어휘를 내 훅이 잡은 것이라 **recall을 전혀 재지 못한다**(아래 함정 ⓑ 그대로 재발). 정확한 이름은 **"게이트 로직·정밀도 검증"**(임계값 2회·리셋 앵커·침묵 경로가 의도대로 도는가 → 맞음). **recall은 여전히 6/35.**
   **함정 2개:** ⓐ `sys.stdin.read()`는 Windows에서 cp949로 디코딩해 한글이 깨져 영영 미매치 → `sys.stdin.buffer.read().decode("utf-8")` 필수. ⓑ **검증을 내가 만든 합성 transcript로만 하면 순환논리** — 실제 세션 jsonl 리플레이라야 recall/precision이 드러난다.

4. **pick-skill 스킬 거취 (미결)** — 자동 라우팅 규칙(6-b)을 지워 고아가 됨. 스킬 파일은 남겨 뒀고 `/pick-skill` 수동 호출만 가능. 몇 주 써 보고 삭제 여부 판단(재감사 때 함께).

5. **10-b 세션 상태 블록 (완료, 2026-07-11 저녁)** — 푸시·완료 제안 시 세션 종료 가능 여부를 명시하는 산출물. 그날 저녁 세 번 고쳤고 **교훈이 하나로 수렴한다: 나는 프로즈보다 가장 가까운 구체 템플릿을 베낀다.**
   - 규칙 7에 프로즈로만 있던 "항목 사이 빈 줄"·"`프롬프트:` 라벨"이 반복 누락됐다 — 원인은 성향 부족이 아니라 **10-b 템플릿 자체가 위반을 모델링**(item 2에 라벨 없음). → 프로즈를 더 쓰지 말고 **템플릿을 고쳐라**(`4a7e7a2`).
   - 권장 항목을 마크다운 순서목록(`1.`)으로 쓰면 렌더러가 하위 `프롬프트:` 줄까지 재번호해 **부모도 1, 자식도 1**이 된다 → **굵은 수동 번호(`**1.**`)** 를 쓴다(`8f800c4`).
   - 남은 일이 있을 때만 선택창(`AskUserQuestion`, "지금 이어서 할까")을 블록 **뒤에** 띄운다. 선택창이 블록을 **대체하면 안 된다** — 라벨이 짧아 복붙 프롬프트 전문(백지 컨텍스트용)을 못 담는다(`576bd8a`).

6. **클로드 웹 시스템 프롬프트 개정 (미착수 — 다른 도구에서 할 일)** — 사용자가 claude.ai 웹에서 자주 쓰는 범용 시스템 프롬프트가 **이미 죽은 것으로 측정된 조항을 나르고 있다**: "유일한 방법이라고 단정하지 않습니다 / 다른 접근이 있을 수 있음을 함께 언급합니다" = 옛 규칙 4 "No only way"(173세션 발화 0회로 잘림). 강제 산출물 `검토한 대안: {X — 버린 이유} / 없음(왜)`으로 교체해야 한다. 함께: "비유 필수" → `**비유:**` 라벨 강제 형태로, 그릇 조항(표/트리/목록) 추가. ⚠ 이건 이 repo가 아니라 **웹 UI에서 사용자가 직접 바꿔야** 하므로 세션 상태 블록에 넣지 않는다(규칙 10-b).

**memory 구조(2026-07-11 확인):** memory는 **전역이 아니라 프로젝트별**이다(`projects/<절대경로 인코딩>/memory/`). 15개 프로젝트가 각자 memory를 갖고 총 130KB. `~/.claude` repo에 memory를 넣어도 이 프로젝트 것(11KB)만 따라가고, 폴더명이 절대경로를 인코딩해 PC가 바뀌면 키가 달라진다. → **git으로 꺼내지 않는다(로컬 전용 유지).** 전역 가치가 있는 사실은 memory가 아니라 **CLAUDE.md로 승격**하는 게 이미 작동 중인 올바른 경로다(feedback_answer_shape → 규칙 7).

**규칙 번호 제약:** `4-b`·`4-c`·`7-b`·`8`·`9`·`11`·`11-b`·`11-c`·`12`는 외부 스킬 9곳(self-review·reference-repos·doc-sync·skillify SKILL.md, wiki, README)이 번호로 참조 중 → **재번호 금지**. 번호 4·5가 빈 것은 의도된 삭제 흔적.

관련: [[project-reference-wiki-migration]]
