# memory 프로젝트 서랍 11건을 각 repo CLAUDE.md로 이관 (규칙 10-c 소급 적용)

완료: 2026-09-05 22:10

생성: 2026-09-05 18:32

## 배경

2026-09-05 세션에서 「전역 설정에 과도한 문서 기록이 있는지」 감사를 했다. 결함 4건이 나왔고 그중 ①②④는 그 세션에서 처리했다. **이건 남은 ③이다.**

전역 규칙 10-c(2026-08-26 도입)는 «프로젝트 사실은 memory가 아니라 그 repo의 `CLAUDE.md`에 쓴다»로 정했는데, **그 이전에 쌓인 서랍 파일들은 이관 규칙이 없어 그대로 남았다.** 결과로 한 프로젝트에 출처가 둘(repo `CLAUDE.md` + `~/.claude/memory/projects/<이름>/`)이고, 서랍 쪽은 아무도 안 열어 낡아간다.

실제 피해 사례가 같은 감사에서 2건 확인됐다(그 세션에서 이미 삭제·정정 완료):
- `Easy_CAD/project_web_pivot_experiment.md` — 「당분간 웹에 집중」인데 repo `CLAUDE.md:113`은 「웹 트랙 일시 중단, Python 집중」. **정반대 방향을 지시**하고 있었다.
- `youtube/user_git_identity.md` — 커밋 이메일이 실측과 반대로 적혀 있었는데 서랍에 갇혀 4개월 방치.

## 대상 11건

경로는 전부 `~/.claude/memory/projects/` 하위.

| 서랍 | 파일 | 이관처 후보 |
|---|---|---|
| Easy_CAD_260718 | `ai-assist-ui-pattern.md` | repo `CLAUDE.md` UI 섹션 |
| Easy_CAD_260718 | `ui-simplicity-principle.md` | repo `CLAUDE.md` UI 섹션 |
| Easy_CAD_260718 | `canvas-point-terminology.md` | repo `CLAUDE.md` 용어 |
| Easy_CAD_260718 | `bug_symbol_library_test_leak.md` | repo `docs/` 또는 삭제(무죄 확정 종결) |
| KBS_Monitoring_v2_260418 | `feedback_no_clear_signal_on_read_none.md` | repo `CLAUDE.md` 주의사항 |
| KBS_Monitoring_v2_260418 | `feedback_sysmonitor_label_format.md` | repo `CLAUDE.md` 주의사항 |
| KBS_Monitoring_v2_260418 | `project_black_recovery_telegram_missing.md` | ⚠ 아래 참조 |
| KBS_Monitoring_v2_260418 | `project_embedded_audio_unmute_on_restart.md` | ⚠ 아래 참조 |
| KBS_Monitoring_v2_260418 | `project_gdrive_folder_lock_on_delete.md` | repo — 단 경로 이전 후 유효한지 확인 |
| KBS_Monitoring_v2_260418 | `project_moved_from_gdrive_to_local.md` | repo — 경로가 `C:\Users\minwoo\Dev\`(다른 PC 사용자명)이라 PC 명시 필요 |
| youtube_dual_subtitle | `gemini_byok_backend.md` | 중복 — repo `CLAUDE.md` §5가 이미 담고 있음, 대조 후 삭제 |
| AI_Dictionary_260622 | `new-pc-shortcut-rebind.md` | repo `CLAUDE.md` 셋업 항목 |

## ⚠ 먼저 확인할 것 — KBS 2건

`project_black_recovery_telegram_missing`(블랙 복구 텔레그램 알림 누락)·`project_embedded_audio_unmute_on_restart`(임베디드 오디오 음소거 저절로 풀림) 둘 다:
- 상태 표기가 **「미해결·관측 대기」이고 2026-08-05 이후 재확인이 없다.**
- repo(`Dev/KBS_Monitoring_v2_260418/CLAUDE.md`·`docs/*.md`)에 **대응 기록이 아예 없다**(2026-09-05 grep 0건 확인). 즉 memory가 유일한 기록이므로 **삭제 금지, 이관 전용.**
- 그대로 옮기지 말고 **사용자에게 지금도 재발하는지 먼저 물어본다.** 해결됐으면 「해결됨 + 언제·무엇으로」로 바꿔 적고, 여전히면 「미해결, {마지막 관측일}」로 날짜를 갱신해 옮긴다.

## 판단 기준

전역 CLAUDE.md 10-c/10-d의 대칭 기준을 그대로 쓴다:
- **코드 옆에 있어야 할 사실인가 → repo**(이번 대상 전부 여기 해당)
- **그저 이 환경의 성질인가 → memory**(이미 2026-09-05에 7건을 전역 루트로 분리 이동 완료)

**서랍에 남겨 둘 것으로 판정된 것**(재검토 불필요): `AROS/project_d_cimon_s_on_gamsi_pc.md`(감시운용PC 환경 + AROS 종속), `Easy_CAD/ai-gateway-key-location.md`(`.secrets` 경로 = 환경 사실).

## 절차

1. 각 파일을 읽고 대상 repo의 `CLAUDE.md`에서 **이미 같은 사실을 다루는 섹션이 있는지 grep**한다. 있으면 새 섹션을 만들지 말고 그 섹션에 흡수한다(doc-sync 스킬 4-b와 같은 원칙 — 2026-09-05 신설).
2. 이관 후 서랍의 원본 파일을 삭제하고, **그 서랍의 `MEMORY.md` 인덱스 줄도 같은 턴에 제거**한다(전역 규칙 10-f, 2026-09-05 신설). 검증: `grep -c "^- \[" <MEMORY.md>` = 파일 수.
3. 서랍이 비면 폴더째 `rmdir`.
4. repo 쪽 변경은 각 repo에서 커밋(규칙 10-a 승인 후). `~/.claude` 쪽 memory 변경은 세션 종료 훅이 자동 커밋한다.

## 참고

- 이 작업의 짝: `20260905-1833-repo-claude-md-재구조화.md`(같은 감사의 결함 ⑤). 순서는 무관하나, 이관을 먼저 하면 재구조화 때 넣을 내용이 확정돼 있어 편하다.
- 감사 전문은 이 세션 대화에만 있고 별도 문서로 안 남겼다. 필요한 결론은 전부 이 파일과 짝 파일에 옮겨 적었다.
