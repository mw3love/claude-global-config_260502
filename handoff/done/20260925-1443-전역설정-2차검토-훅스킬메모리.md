완료: 2026-09-25 14:56

# 전역설정 2차 검토 — 훅·스킬·메모리

생성: 2026-09-25 14:43

## 배경

2026-09-25에 전역 `CLAUDE.md`를 Opus 5.5에 맞춰 줄였다(커밋 `8974536`). Claude가 매번 읽는 분량이 27,100자에서 6,126자(152줄)로 줄었고, 옛 원문은 규칙마다 HTML 주석으로 남겨 뒀다.

- 사용자가 정한 목표(100): 답이 쉽게 이해되는 것. 중학생 눈높이. "쉽게 말해줘"를 다시 할 필요가 없는 상태.
- 사용자의 현재 불편(1): 말이 어렵다. 답이 길고 형식이 많다.
- 방침: 과감하게 뺀다. 문제가 생긴 것만 되살린다(git으로 되돌릴 수 있음).
- 진단 페이지(1차 결과, 근거, 공식 문서 인용): https://claude.ai/artifact/HfEXcb7oYc5Cx6eJziGhDW
- 개편 메모와 성적표 측정 방법은 `~/.claude/CLAUDE.md` 맨 위 주석에 있다.

## 이번에 할 일

1차에서는 `CLAUDE.md`만 봤다. 같은 방식(유지 / 줄이기 / 합치기 / 빼기, 근거는 사용 기록 + 공식 문서)으로 나머지를 검토한다.

1. **스킬 설명(description)**: 매 세션 시스템 프롬프트에 실리는 부분이다. 긴 설명을 줄일 수 있는지 본다. 본문 크기: design-bakeoff 28.6KB, doc-sync 15.0KB, kairos 11.6KB, reference-repos 10.9KB. 호출 빈도는 `python ~/.claude/tools/audit_rules.py`로 볼 수 있다(doc-sync 49, handoff 11, self-review 8, design-bakeoff 5, reference-repos 3, deep-interview 3, jbnu-gateway 2, sync-repos 1. hwp-from-data·skillify·kairos-proposal·draft는 0).
2. **훅 6종**: `settings.json`의 SessionStart·Stop·SessionEnd·PostToolUse·UserPromptSubmit·PreToolUse. 각 훅이 주입하는 문구가 새 `CLAUDE.md`와 맞는지 본다. 특히 stuck-loop-hook의 `[격상 게이트]` 문구는 개편 때 건드리지 않았다.
3. **메모리**: `MEMORY.md`는 31줄이다. 낡은 항목이 있는지, `CLAUDE.md`와 겹치는 항목이 있는지 본다(예: `feedback_answer_shape.md`와 새 규칙 7).
4. **`tools/audit_rules.py` 지문 갱신**: 개편으로 몇 개가 옛 표현을 찾게 됐다. '손안의 카드'(강제 한 줄 삭제), self-review 자동실행(규칙 11 삭제). 이대로 두면 "사문화"로 잘못 나온다.

## 판단 기준

- 사용자에게는 결과를 쉬운 말로 설명한다(새 규칙 7). 필요하면 1차처럼 HTML 한 장으로 보여 준다.
- 적용 전에는 확인을 받는다. 사용자는 계획과 결정 단계를 이해하고 싶어 한다.
- 공식 문서 근거: code.claude.com/docs/en/memory, platform.claude.com의 prompting-claude-opus-5 / prompting-claude-opus-5-5.

## 별도: 기간이 지나야 하는 일(이 파일로 처리하지 않음)

2026-10 초에 성적표를 잰다. 기준은 "쉽게 말해줘" 비율이 4.6%에서 1%대로 내려갔는지, 그리고 사용자가 `/self-review`를 다시 손으로 시키기 시작했는지다. 사용자가 손으로 시키기 시작했다면 자동 재검토를 되살린다.
